---
trdd-id: S9NKP4WQ
title: CC spec-sync v2.1.192–2.1.200 — 2 allowlist FP-fixes (manual perm-mode + Notification agent triggers)
column: dev
created: 2026-07-03T21:49:42+0200
updated: 2026-07-03T21:49:42+0200
current-owner: cpv-main-session
task-type: bugfix
release-via: publish
test-requirements: [unit]
relevant-rules: []
---

# TRDD-S9NKP4WQ — CC spec-sync v2.1.192–2.1.200 (2 allowlist FP-fixes)

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative) — 2026-07-03

- **What/why:** CPV had fallen behind the Claude Code plugin spec (v2.1.192–v2.1.200). A full
  audit (report below) found exactly 2 gaps — both pure allowlist-widening FP-fixes (FN-safe by
  construction: we ADD newly-valid values, remove nothing). Directly on CPV's mandate (reduce FPs).
- **Gap 1 (MAJOR false-positive) — permission mode `manual`:** CC v2.1.200 made `"manual"` a valid
  `defaultMode` / `--permission-mode`. CPV's `VALID_PERMISSION_MODES` omits it, so CPV WRONGLY
  rejects a valid plugin. **Fix:** add `"manual"` to the frozenset at
  `scripts/cpv_validation_common.py:423`.
- **Gap 2 (INFO) — Notification hook triggers:** CC v2.1.198 added `agent_needs_input` /
  `agent_completed` Notification triggers. CPV's `COMMON_NOTIFICATION_TYPES` omits them.
  **Fix:** add both to the set at `scripts/validate_hook.py:148`.
- **Deltas 1,4,5,6,7,8** (hyphenated-matcher substring guidance, `claude-sonnet-5` model allowlist,
  settings.json keys, `Agent(...)` permission grammar, skill-frontmatter casing + comma matchers,
  CPV-own-component impact) — already-handled or N/A. No change.
- **NEXT ACTION:** apply the 2 additive fixes + two-sided unit tests, cache-cold self-validate, publish.
- **Durable evidence:** `reports/spec-sync-audit/20260703_214716+0200-cc-2192-2200-audit.md`.

## Plan

1. Add `"manual"` to `VALID_PERMISSION_MODES` (`cpv_validation_common.py:423`).
2. Add `"agent_needs_input"`, `"agent_completed"` to `COMMON_NOTIFICATION_TYPES` (`validate_hook.py:148`).
3. **Two-sided tests (FN-safe):** (a) a plugin/settings with `defaultMode: "manual"` → NO LONGER
   flagged, AND a bogus mode (`"banana"`) → STILL flagged; (b) a Notification hook using
   `agent_completed` → recognized as a known trigger, AND existing unknown-trigger behavior
   unchanged for a bogus type. Follow the house test convention.
4. Verify: `ruff` + `mypy` clean; cache-cold `CPV_SCAN_CACHE=0 … --strict` self-validate 0/0/0/0;
   regen self-hashes LAST; `publish.py --patch` (or fold into the #155 `--minor`); CI green.

## Acceptance

- `manual` accepted (no FP); invalid modes still rejected (two-sided).
- `agent_needs_input`/`agent_completed` recognized; unknown-trigger handling unchanged.
- ruff+mypy clean; CPV self-validate clean; CI green.

## Approval log

- 2026-07-03 — User directed the CC spec-sync ("align the plugin with the following claude code
  changes"). Audit-confirmed 2 FP-fixes; FN-safe allowlist-widening. Authorized under go-on-yourself.
