---
trdd-id: c1924215-f709-4e51-b589-41e0c7c9acca
title: Issue #71/#72 — bundled-Rust-CLI false positives block --strict
column: published
created: 2026-06-10T00:27:09+0200
updated: 2026-06-10T00:27:09+0200
current-owner: cpv-maintainer-claude
assignee: cpv-maintainer-claude
task-type: bugfix
release-via: publish
publish-target: claude-plugins-validation
test-requirements: [unit, lint, typecheck]
relevant-rules: []
external-refs: ["github.com/Emasoft/claude-plugins-validation/issues/71", "github.com/Emasoft/claude-plugins-validation/issues/72"]
---

# Issue #71/#72 — bundled-Rust-CLI false positives block --strict

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative) — 2026-06-10

**Current state:** SHIPPED. #71/#72 (3 FP classes) FIXED + published v2.126.2
(CI green, both CLOSED). The same session also shipped #73 (binary-asset
prose-scan, v2.126.3, CLOSED) and #69 (variable-Bearer TOKEN_STEAL dead-code
fix, v2.126.4, CLOSED) — see "Related open issues" below. #70(A) verified
fixed; #70(B)/(C) status-commented, kept OPEN for reporter retest. All fixes
two-sided tested (+27 tests test_issue_71/73/69); serial suite 8884 pass;
self-validate 0/0/0/0. No further action unless #70 reopens. NEXT: none.

**Fixes (FN-safe precision — no rule deleted, no --strict relaxed):**

1. **Bug 1 — private-path/absolute-path scan walked build-output dirs.**
   `scripts/cpv_validation_common.py`: added `.next` + `vendor` to
   `PRIVATE_INFO_SKIP_DIRS` (already had target/dist/build/node_modules/.venv/
   __pycache__), and changed `extra_skip = skip_dirs or set()` →
   `extra_skip = (skip_dirs or set()) | PRIVATE_INFO_SKIP_DIRS` in BOTH
   `scan_directory_for_private_info` AND `validate_no_absolute_paths`. The
   walk prunes by basename (`gitignore_filter._walk_pathlib`:
   `if entry.name in skip_dirs`), so a nested `tools/x/target/` is now caught
   regardless of the root-only GitignoreFilter. FN-safe: a leaked path in a
   TRACKED file (any dir NOT in the build/dep set) still fires.

2. **Bug 2 — SHELL_EXEC over-fired on Rust `eval` identifiers.** New
   `scripts/_skillaudit_rust_context.py` (`classify(...)` →
   `safe_literal`/`unknown`). Registered `.rs` in `_CLASSIFIER_EXTENSIONS`
   and added the `.rs` dispatch branch in
   `cpv_skillaudit_native._context_classifier_verdict`. Suppresses the
   `eval`-token SHELL_EXEC match (Rust has no shell/runtime-eval builtin);
   real exec (`Command::new`/`std::process`/`.spawn(`/`.output(`/`.status(`/
   `.exec(`) falls through and fires (the `spawn(` pattern). re2-safe (no
   lookaround in the classifier regexes). Python builtin `eval(` in `.py`
   still fires (carve-out is `.rs`-gated).

3. **Bug 3 — AGENT_MEMORY_MOD over-fired on a memory-authoring skill.** New
   `_is_memory_authoring_skill(file_path, content)` +
   `_MEMORY_TERM_RE`/`_MEMORY_AUTHORING_TERM_RE`/`_MEMORY_ATTACK_INTENT_RE`
   in `cpv_skillaudit_native.py`, plus an early `return "suppress"` carve-out
   in `_context_classifier_verdict` (next to the CLAUDE_CLI_UNAUTHORIZED_INSTALL
   one). Suppresses AGENT_MEMORY_MOD when the skill's OWN frontmatter
   name/description declares memory authoring (memory term AND authoring verb,
   voided by an attack-intent term). FN-safe: a non-memory skill, or one that
   openly describes injecting/hijacking ANOTHER agent's memory, still fires
   (demoted-but-visible).

4. **Docs** — `skills/plugin-validation-skill/references/plugin-structure.md`
   gains a "Bundled native binaries (Rust / Go / compiled tools)" subsection
   documenting the canonical `rust/` + `bin/` + `build.sh` layout (the #71
   cross-cutting `tools/` MAJOR ask). CPV already recognises `rust/` and
   `bin/` as standard dirs (validate_plugin.py:1886/1937) — `tools/` stays
   correctly flagged.

**Load-bearing learning — the scan cache is keyed by VERSION, not classifier
code.** The v2.104.0 skillaudit result cache keys on
`(content_hash, _CATALOG_HASH, __version__, file_ext)`. A classifier-code
change at the SAME version is masked by a cache hit, so local testing of a
classifier fix MUST set `CPV_SCAN_CACHE=0` (the new test file's autouse
fixture does this). At publish the version bump is the cache epoch — it
always invalidates between releases, so USERS get the fix. Defensible
as-is (version = cache epoch, guaranteed by the always-bump-on-publish rule);
NOT changing the cache key to hash classifier modules (per-scan cost for zero
user benefit).

## #72 alignment

Issue #72 is the change PROPOSAL for #71's 3 classes. The implementation matches
it precisely: Proposal 1 option (b) build-dir skip (+ `.next`/`vendor`),
Proposal 2 the `.rs` `eval`-method precision bump keeping Command/.spawn/
.output/.status firing, Proposal 3 the defensive-documentation memory-skill
lens — using the exact test fixtures #72 listed. Closing #71 and #72 together.

## Verification

- Fixture `/tmp/rt71.bdvSHI` (cache OFF): Bug 1 → 0 `target/` private-path
  findings (was 6 CRITICAL), Bug 2 → only `predicate.rs:9` real spawn fires
  (`:3 fn eval` + `:6 .eval` cleared), Bug 3 → 0 AGENT_MEMORY_MOD on the
  memory skill (NIT 0).
- `tests/test_issue_71_rust_bundled_fp.py` — 12 two-sided tests (all green):
  build-dir-skip plus tracked-still-fires (private + absolute), Rust eval
  no-fire plus real Command.spawn fires plus Python eval still fires,
  memory-skill suppress plus non-memory/attack-intent still fire.
- ruff check ✓, ruff format ✓ (my lines; pre-existing v2.125.0 banner format
  drift left untouched — lint gate doesn't run `ruff format --check`),
  mypy ✓.

## Related open issues — verified status (2026-06-10)

- **#73** — binary `.ttf` font text-scanned → CRITICAL INDIRECT_PROMPT_INJECT
  on glyph bytes. REPRODUCED + FIXED in v2.126.3: new
  `_BINARY_INAPPLICABLE_RULES` + extended `_suppress_binary_placeholder`
  suppress prose-instruction / source-shape rules on binary-scan findings;
  real exfil / secret / exec / decode still fire (verified DATA_EXFIL on a
  webhook URL in a binary still fires). +6 two-sided tests
  (`tests/test_issue_73_binary_asset_fp.py`).
- **#70(A)** — `validate_plugin --json` writing repo lint to stdout. VERIFIED
  FIXED on current code: `--strict --json` stdout is pure JSON (`json.loads`
  succeeds, 0 REPO LINT banner). (B) self-scanner-match + (C) orphan-cache
  integrity — ask reporter to retest on v2.126.2+.
- **#69** — skillaudit FPs on governance / auth docs. 3/4 classes were
  already clean (persona-curl `Authorization: Bearer` in a code span,
  a2a-cross-inject doc, intent-destructive doc — the v2.124.1 sink-aware fix).
  Residual FIXED in v2.126.4: `_is_bearer_token_placeholder`
  (`_skillaudit_markdown_context.py`) lowercased the Bearer value BEFORE the
  "short uppercase identifier" heuristic, making that check DEAD CODE —
  `$AID_AUTH` / `YOUR_API_KEY` were never recognised, so TOKEN_STEAL demoted
  to a publish-blocking NIT. Now the uppercase-name check runs on the
  ORIGINAL case, gated on a `$`/`{` marker OR an embedded `_`, so a variable /
  constant-name Bearer value clears while a literal JWT and a contiguous
  uppercase secret blob (AWS key id) still demote. +9 two-sided tests
  (`tests/test_issue_69_bearer_var_fp.py`).
