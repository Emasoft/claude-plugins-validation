---
name: cpv-create
description: Interactive creation — create plugins, marketplaces, publish to GitHub
agent: plugin-creator
argument-hint: "[what_to_create]"
user-invocable: true
---

Create or publish Claude Code plugins and marketplaces.

If a task is described in $ARGUMENTS, start that workflow.
Otherwise, ask the user what they want to create or publish.
