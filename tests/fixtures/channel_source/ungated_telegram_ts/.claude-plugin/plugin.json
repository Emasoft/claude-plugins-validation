{
  "name": "ungated-telegram",
  "version": "1.0.0",
  "description": "Vulnerable Telegram channel — no sender gating",
  "mcpServers": {
    "telegram": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/servers/telegram/dist/server.js"]
    }
  },
  "channels": [
    {"server": "telegram"}
  ]
}
